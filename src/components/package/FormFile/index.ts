// import { App, Plugin } from 'vue'

import FormFile from './FormFile.vue'

// export default {
//     install(Vue: App) {
//         Vue.component(FormFile.name, FormFile)
//     }
// } as Plugin

export {
    FormFile
}